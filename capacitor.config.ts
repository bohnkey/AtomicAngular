import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atomicangular.app',
  appName: 'atomic-angular',
  webDir: 'dist/atomic-angular/browser'
};

export default config;
